# Python 每日一学 · 学习档案

> 本文件与豆包工作会话「Python 刷题学习方案」同步，沉淀每天的学习任务、AI 批改结果、薄弱点与订正项，作为 GitHub 仓库内的每日学习档案。
>
- **学习目标**：基于 [Python-100-Days](https://github.com/jackfrued/Python-100-Days) 和尚硅谷《Python2.0》讲义，30 天从「看得懂」练到「写得出」，以练代学。
- **学习模式**：每天一个主题，先「知识速整」（用自己的话写一句话笔记 + 亲手敲 3~5 行最小示例），再「分层刷题」（基础必做 + 进阶对应薄弱点），学:练 ≈ 2:8。
- **闭环**：定时任务每天 07:00 推送当日任务 → 手写代码跑通 → 发回批改 → 错题/薄弱点登记进档案，隔天优先复习；订正完再推进新内容。
- **代码位置**：每日「知识速整 + 分层题目」按天组织在 `review/1day/`、`review/2day/`、`review/3day/`、`review/4day/`、`review/5day/`（dayXX_review.py / dayXX_practice.py + README 知识卡）；主题笔记写在 `学习/` 对应目录；跨主题方法地图汇总在 `review/knowledge_organize.md`。

---

## 第一周计划（D1–D7，对齐尚硅谷讲义 16 章）

| 天 | 主题 | 讲义章节 | 状态 |
| --- | --- | --- | --- |
| D1（09-14） | 基础知识 + 流程控制 | 第 3–4 章 | 已完成 |
| D2（09-15） | 容器数据类型（重点多练，一天覆盖五类容器） | 第 5 章 | 已完成，小实战已订正（有小语法问题），Q2/Q8/Q10 仍待订正 |
| D3（09-15 晚） | 函数 | 第 6 章 | 已完成（Q1–Q8，Q9 汉诺塔未完成，Q10 有拼写错误） |
| D4（09-16 中午） | 文件操作 + 错误和异常 + JSON | 第 7、11 章 | 已完成（copyFile + Q1–Q3，Q4/Q6/Q7/Q8 待还债巩固） |
| D5（09-16 下午） | 面向对象 | 第 8–10 章 | 知识已学，练习已完成（W1–W7 + Q1–Q8） |
| D6（09-17~19） | 模块包 + 迭代器/生成器 + 装饰器（正则了解） | 第 12、13、15 章 | 已完成（W1–W7 + Q1–Q10，含 my_math 包练习） |
| D7（09-19~20） | 进程线程 + 第一周周测 | 第 14、16 章 | 已完成（W1–W5 + Q1–Q10，含 D6 白笔默写、D4 换皮复测） |

> 赶进度规则：按实际完成进度推送，已完成的内容不重复发。D2 即为赶进度时提前领取，一天完成了原 30 天计划中 Day2–Day5 的全部容器内容；D3–D5 继续赶进度，09-15 晚到 09-16 下午三天完成了原计划 Day6–Day15 的函数、文件操作异常、面向对象知识。

---

## D1 · 09-14 · 基础知识 + 流程控制

**产出**：[review/day01_review.py](review/1day/day01_review.py)（同步副本：`学习/1 基础语法知识/`、`学习/2 流程控制语句/`）、[刷题/自主做题/P01_1day.py](刷题/自主做题/P01_1day.py)

### 知识速整覆盖点
变量与命名 / 数据类型与互转 / 进制（bin·oct·hex·ord·chr）/ 输入输出（sep·end·f-string）/ 运算符（含 is 与 ==、短路求值、海象运算符）/ 分支（if-elif-else、match-case、三目）/ 循环（for·range·while·break·continue·pass·循环 else）。

### 摸底测评（10 题完成 3.5 题）暴露的问题
- Q1 交换变量：两种写法都对，但漏了 `float("3.14")`、`bin(42)` 第二问。
- Q2 成绩等级：elif 链思路对；「报错」要真的 `raise ValueError(...)`，`print` 不算报错；代码被注释没运行。
- Q3 素数：`for...else` 用对了；但没运行、没实现「每行 10 个」。
- Q4 列表去重：边遍历边 `remove` 会漏元素/死循环，当时写崩（D2 已攻克，写出 4 种去重）。
- Q5–Q10（字典统计、函数、递归、文件异常、类、装饰器）当时空着，已排进 D2–D6。

### 已做题订正要点
- 接数字用 `int(input())/float(input())`，不要用 `eval(input())`（会把输入当代码执行，有安全隐患）。
- 九九乘法表下三角用 `range(1, 10)`，`range(9, 0, -1)` 是倒序。
- 不要用内置函数名 `sum` 当计数器变量。
- 取整数每一位：`a = i % 10`（个位）、`b = i // 10 % 10`（十位）、`c = i // 100`（百位）；`i % 10 // 10` 恒为 0 是 D1 没打出水仙花数的原因。

### 三个核心「题型 → 工具」映射
| 题目长什么样 | 用什么 | 套路 |
| --- | --- | --- |
| 求所有方案/组合 | 枚举法 | 多层 for 枚举 + if 筛选（如百钱百鸡） |
| 求各有多少个 | 计数器法 | 遍历 + 每类一个变量，符合就 +=1（如字符统计） |
| 取整数每一位 | 取模/整除 | `%10` 取末位，`//10` 砍末位 |

---

## D2 · 09-15 · 容器数据类型（list / str / tuple / set / dict）

**产出**：[review/day02_review.py](review/1day/day02_review.py)；主题笔记 `学习/3 容器数据类型/` 下 [P01_List.py](学习/3%20容器数据类型/P01_List.py)、[P02_String.py](学习/3%20容器数据类型/P02_String.py)、[P03_Tuple.py](学习/3%20容器数据类型/P03_Tuple.py)、[P04_Set.py](学习/3%20容器数据类型/P04_Set.py)、[P05_Dict.py](学习/3%20容器数据类型/P05_Dict.py)、[read.txt](学习/3%20容器数据类型/read.txt)。

### 知识速整覆盖点
序列共性（索引/切片/len/in/遍历）；列表增删改查、sort vs sorted、推导式、zip；字符串 split/join/replace/strip/find/count、大小写与判断；元组单元素 `(1,)`、打包解包、多返回值；集合交并差与去重；字典 `d[k]` vs `get`、setdefault/update/pop、items 遍历；四类容器对比表。

### 批改总览
| 题 | 结果 | 说明 |
| --- | --- | --- |
| 速整笔记 | ✓ | append/extend、remove/discard、get 缺省返回 None 都记准了 |
| Q1 列表操作 | ✓ | 流程全对 |
| Q2 推导式 | ✗ | a)、c) 有错，需订正 |
| Q3 字符串 | △ | split 没按题目要求的逗号分 |
| Q4 元组 | ✓ | 漏验证 `(1)` 与 `(1,)` |
| Q5 集合 | ✓ | 写出 4 种去重，超预期；有一个坏味道 |
| Q6 字典 | △ | 两处小问题 |
| Q7 词频统计 | ✓✓ | D1 卡点，本次独立写对，setdefault + lambda 排序正确 |
| Q8 二维列表 | ✗ | 只做 1/3，还覆盖了原数据 |
| Q9 数位 | ✓ | 十位 `i%100//10` 正确，水仙花结果对；补一个数学法 |
| Q10 字符统计 | △ | 「不区分大小写」「最多的字母」两个要求没完成 |
| 小实战 | ✗ | 学生成绩管理，未做 |

### 必须订正的 3 题（订正完再进 D3）

**Q2 列表推导式**——偶数条件被写成了「被 3 整除」，且 c) 没用推导式生成新列表：
```python
print([i**2 for i in range(1, 101) if i % 2 == 0])   # a) 偶数平方
print([i**2 for i in range(1, 101) if i % 6 == 0])   # b) 偶数里再筛被3整除 = 被6整除
print([s.lower() for s in ["AbC", "dEf"]])           # c) 推导式直接产出列表
```

**Q8 二维列表**——补齐「每人平均/总平均」，且不要用 `list1 = []` 覆盖原始成绩：
```python
scores = [[88, 76, 92], [66, 77, 88], [99, 91, 85]]
print("每人平均:", [round(sum(r)/len(r), 2) for r in scores])       # [85.33, 77.0, 91.67]
print("每科平均:", [round(sum(c)/len(c), 2) for c in zip(*scores)]) # [84.33, 81.33, 88.33]
print("总平均:", round(sum(sum(r) for r in scores)/9, 2))           # 84.67
```

**Q10 字符统计**——键用 `ch.lower()` 实现不区分大小写；用 `key` 同时取到「哪个字母」最多：
```python
d = {}
for ch in s:
    if ch.isalpha():
        k = ch.lower()
        d[k] = d.get(k, 0) + 1
print(max(d.items(), key=lambda x: x[1]))   # 同时拿到字母和次数
```

### 5 个顺手改的小问题
1. Q3：按逗号分用 `s.split(",")`，对比 `split()` 按任意空白分的区别。
2. Q4：补 `print(type((1)), type((1,)))`，前者是 int、后者才是元组。
3. Q5：`[new.append(x) for x in ...]` 是坏味道——推导式用来「生成新列表」，靠它做 append 副作用只会得到全 None 的列表，这种场景写普通 for 循环。
4. Q6：`nam1e/ag1e` 笔误导致 update 变成新增键而非覆盖；遍历时解包 `for k, v in d.items()`。
5. Q9：字符串拆位 `[int(c) for c in str(n)]` 可以，再敲一遍数学法：
```python
def digits(n):
    res = []
    while n > 0:
        res.append(n % 10)   # 取末位
        n //= 10             # 砍末位
    return res[::-1]         # 倒回来
```

### 两个要盯住的习惯
- **审题逐条对账**：做完把题目每个输出点圈出来对一遍——D2 的错里多数是「会做但漏做」。
- **别覆盖正在用的变量**（如 Q8 的 `list1 = []`），也别用推导式干副作用的活。

### D2 待办
- [ ] 订正 Q2、Q8、Q10（仍未订正，推进 D3 时跳过了，需回头补）
- [x] 完成小实战：学生成绩管理（已订正，输出人数/总分/平均分/最高分/降序名单）
  - ⚠ 遗留小问题：`print(sum(dic.values()) / len(dic), 2)` 里逗号会被当成 print 的两个参数，输出 `(平均分, 2)` 而不是保留两位小数；应改为 `print(round(sum(dic.values()) / len(dic), 2))`
- [ ] 订正发回复查通过后，进入 D3 函数专题（已提前进入 D3，D2 订正项挂起待补）

---

## D3 · 09-15（晚）· 函数

**产出**：[review/2day/day03_review.py](review/2day/day03_review.py)、知识速整 [review/2day/readme.txt](review/2day/readme.txt)（函数部分）、方法地图汇总 [review/knowledge_organize.md](review/knowledge_organize.md)

### 学了什么
函数定义与调用（def 语法、先定义后调用、docstring）；形参 vs 实参、值传递 vs 引用传递（不可变类型地址变化、可变类型地址不变值变化）；参数五种形式（位置 / 默认 / 关键字 / 可变位置 `*args` 打包元组 / 可变关键字 `**kwargs` 打包字典）；解包传参（`*list`、`**dict`）；强制位置/关键字参数（`/` 前必须位置、`*` 后必须关键字）；返回值（无 return 返回 None、多值返回是元组、解包接收）；闭包三要素（函数嵌套 + 内部函数访问外层变量 + 外层返回内部函数对象）；作用域 LEGB（Local→Enclosing→Global→Built-in，只有 module/class/def 引入新作用域，if/for/try 不引入）；global 改全局、nonlocal 改外层嵌套；递归三要素（出口 + 递推式 + 规模缩小，阶乘示例）；匿名函数 lambda（`lambda 参数: 表达式`，常作 sorted/max 的 key）；高阶函数 map（逐一处理）、filter（过滤）、reduce（累积，需 from functools import）。

### 做了什么
- Q1 max2(a,b) 返回较大者（一行条件表达式 `a if a>b else b`）
- Q2 register(name, age=18, *hobbies, **info) 多参数演练，三种调用方式（位置/关键字/混合）
- Q3 默认参数可变对象陷阱：`def add(x, lst=[])` 运行两次观察 `lst` 累积（亲眼看到默认参数在函数定义时创建一次、后续调用共享）
- Q4 calc(a,b) 返回 a+b/a-b/a*b 三个值，解包接收
- Q5 change() 用 global 把全局 count 加 1，调用 3 次
- Q6 递归阶乘 fact(n)，跑 fact(5)=120
- Q7（D2 回炉）成绩字典多级排序：按分数降序、并列时按姓名升序，`key=lambda x: (-x[1], x[0])`
- Q8 闭包计数器 make_counter()，用 nonlocal 修改外层变量，每次调用 +1

### 有什么问题
- **Q9 汉诺塔未完成**：hanoi(n, a, b, c) 递归经典题，只写了开头没写完。递归思路：把上面 n-1 个从 a 移到 b（借助 c）→ 把最下面 1 个从 a 移到 c → 把 n-1 个从 b 移到 c（借助 a）。
- **Q10 函数化改造有拼写错误**：把成绩管理改写成 get_scores() + analyze(d) 两个函数时，函数名写成了 `analuze`（应为 `analyze`）；且 get_scores() 里输入 q 时把 dict1 重置为空再 break，导致返回空字典。
- **递归斐波那契对比耗时未做**：Q6 要求再写递归版斐波那契和迭代版对比 n=35 耗时，只做了阶乘，没做斐波那契耗时对比（递归版 n=35 会明显慢，因为大量重复计算）。
- **默认参数陷阱理解了但要记住**：默认参数用不可变类型（None/数字/字符串），不要用 []/{} 这类可变对象。

---

## D4 · 09-16（中午）· 文件操作 + 错误和异常 + JSON

**产出**：[review/2day/day04_review.py](review/2day/day04_review.py)、知识速整 [review/2day/readme.txt](review/2day/readme.txt)（文件操作+异常+JSON 部分）；练习产物 `review/2day/chat.txt`、`scores.json`、`access.log`、`history.json`、`response.json`

### 学了什么
文件概念（文本/二进制、绝对/相对路径）；open 三参数（路径/mode/encoding，Windows 不写 encoding 可能乱码）；with 关键字（自动关闭、异常也关，以后一律用 with）；模式 r/w/a/b（'w' 一打开就清空原文件！r 不存在报错、w/a 会新建）；写（write 返回字符数不自动换行、writelines 同样不自动加换行）；读（read 全读大字符串、readline 读一行含\n、readlines 全读成列表、`for line in f` 逐行读首选大文件不爆内存）；文件拷贝 copyFile（三种写法：逐字节/逐行/分块）；异常体系（语法错误 vs 运行时异常）；捕获结构 try-except-else-finally（else 只有 try 成功才走、finally 无论如何都执行常用于兜底关资源）；别写裸 except（会连 Ctrl+C 都吞，至少 except Exception）；一次捕多种 `except (ValueError, KeyError) as e`；主动抛错 raise、调试断言 assert（条件 False 抛 AssertionError）；异常传递（函数内不捕获会一层层往上抛）；必认 6 异常（FileNotFoundError/KeyError/IndexError/ValueError/TypeError/ZeroDivisionError）；自定义异常（class MyError(Exception)）；json 四兄弟（dumps 字典→JSON字符串、loads JSON字符串→字典、dump 字典→写文件、load 读文件→字典；ensure_ascii=False 让中文不变成 \uXXXX、indent 缩进美化；JSON 里 true/null 小写别和 Python 混）。

### 做了什么
- copyFile 函数写了 3 个版本（逐行读、分块读 1024 字节、with 安全版）
- add(num1, num2) 函数配合异常捕获练习
- 自定义异常类 MyError(Exception)（__init__/__str__ 注释了待完善）
- Q1 写对话记录：列表 3 条写入 chat.txt（w 模式），再用追加模式 a 加第 4 行"AI:好的..."
- Q2 读文件加行号：先 read() 一次性打印，再逐行读用 enumerate(start=1) 输出 `1. 用户:你好` 格式，rstrip('\n') 去行尾
- Q3 安全数字输入：循环输入，数字打印平方，非数字 try-except ValueError 不崩溃提示"请输入有效数字"，输入 q 退出
- 练习产物：chat.txt（4行对话）、scores.json（测试字典数据）、access.log/history.json/response.json（文件操作练习时生成的测试文件）

### 有什么问题
- **Q4/Q6/Q7/Q8 待还债巩固**：D5 作战卡明确要求"明早(D6开工前)合上资料，白纸重写 D4 的 Q4/Q6/Q7/Q8（约30分钟）"，说明这几题还没完全掌握，需要不看资料重写一遍。
- **自定义异常 MyError 不完整**：__init__ 和 __str__ 被注释了，只写了类骨架，没真正实现自定义异常的初始化和字符串表示。
- **json 练习偏浅**：只做了基础的 dumps/loads，dump/load 写文件读文件的完整流程还需要多练（scores.json 是测试数据，不是完整的读写闭环练习）。
- **大文件读取选型要记住**：小文件随便读，大文件（日志/训练数据）只用 `for line in f`，因为 read()/readlines() 会把整个文件载入内存。

---

## D5 · 09-16（下午）· 面向对象

**产出**：知识卡 [review/3day/day05_README.txt](review/3day/day05_README.txt)（完整，含知识速整清单/方法地图/最小示例 W1–W7/易错点/AI开发视角）、练习册 [review/3day/day05_practice.py](review/3day/day05_practice.py)（题目框架完整，W1–W7 热身 + Q1–Q10 正式题，**代码部分待写**）

### 学了什么
类与对象（类是模具、对象是用模具压出来的月饼；class 定义类、类名() 创建对象）；__init__（创建对象时自动调用的初始化方法，self 不用手动传）；self（调用 对象.方法() 时 Python 自动把这个对象传进 self，相当于 Java 的 this）；类属性 vs 实例属性（类属性全类共享一份写在类里方法外、实例属性每个对象各一份写在 __init__ 里 self.x=...；**最大的坑：方法里 self.count += 1 不会改类属性，会偷偷新建同名实例属性，改类属性要用 类名.count += 1**）；三种方法（实例方法带 self 90%普通方法、类方法 @classmethod 带 cls 用于计数器/注册表/备选构造、静态方法 @staticmethod 都不带用于逻辑属于类但不依赖状态）；封装（私有 __x 改名重整成 _类名__x 君子协定不是真锁、单下划线 _x 内部约定、@property 把方法伪装成属性读、@属性名.setter 赋值时校验可 raise）；继承（class 子类(父类)、super() 调父类方法、子类写了 __init__ 必须显式 super().__init__() 否则父类属性建不出来、同名方法=重写 override）；多态与鸭子类型（不看对象是什么类型只看它有没有那个方法，"走起来像鸭子就是鸭子"）；判断类型用 isinstance（子类实例对父类也返回 True）、type 要求严格相等；魔术方法 dunder（__init__ 创建对象、__str__ print/str 时调用必须 return 字符串不能 print、__repr__ 调试用、__len__ len(obj)、__getitem__ obj[i]/切片、__call__ 把对象当函数调；实现 __len__+__getitem__ 对象就能像列表一样被 len/下标/切片/遍历）；了解即可：多继承与 MRO、动态给对象加属性、__slots__ 限制属性。

### 做了什么
- 知识卡 day05_README.txt 完整整理：9 条知识速整清单 + 方法地图（类骨架/类属性vs实例属性/三种方法/封装/继承多态/魔术方法）+ 7 个最小示例（W1 Dog类/W2 亲眼看self/W3 类属性vs实例属性/W4 三种方法同台/W5 @property校验/W6 继承super重写/W7 魔术方法Box）+ 7 条易错点 + AI 开发视角（LangChain ChatOpenAI 继承、chain("问题") 是 __call__、消息列表 len/[-1] 是 __len__/__getitem__、SDK 里 @property/@classmethod from_xxx、_generate 单下划线内部方法约定）
- 练习册 day05_practice.py 题目框架完整：W1–W7 热身跟写（每个有跟写要求+微调）+ Q1–Q10 正式题（Q1 消息类/Q2 __str__/Q3 类属性计数器含坑实验/Q4 封装 LLMClient/Q5 模型家族继承多态/Q6 三种方法同台/Q7 魔术方法打造对话历史容器/Q8 综合简易对话机器人串联 D3–D5/Q9 选做读代码题/Q10 选做 __call__）
- **代码部分尚未开始写**（刚写完知识卡和题目框架，14:05 保存）

### 有什么问题
- **今日盯防三点**（作战卡明确标注）：① self 到底是谁（方法第一参数，Python 自动传入调用对象）；② 类属性 vs 实例属性（self.count += 1 的坑）；③ __str__ 里是 return 不是 print（在 __str__ 里 print 会得到 None）。
- **待还债 D4 的 Q4/Q6/Q7/Q8**：D5 作战卡要求明早 D6 开工前合上资料白纸重写，约 30 分钟。
- **练习代码未开始**：W1–W7 热身和 Q1–Q8 正式题都还没写代码，需要接下来完成（Q9/Q10 选做）。
- **继承 vs 组合要判断**：易错点第 7 条——先判断"该用继承(is-a)还是组合(has-a)"，Bot 有一个 LLMClient 就持有它（组合），别去继承它。Q8 对话机器人就是组合场景。

---

## 额外预习 · 09-15（晚）· 进程与线程（原计划 Day19，提前看）

**产出**：[学习/10 进程与线程/readme.txt](学习/10%20进程与线程/readme.txt)（知识整理）；`学习/10 进程与线程/multiprocessing.Process.py`（空文件，代码待写）

### 学了什么
并发与并行（并发：单个 CPU 处理多个任务各任务交替执行；并行：多个 CPU 同时执行多个任务）；同步与异步（同步：排队依次执行；异步：同时执行互不干扰）；进程概念（资源分配的基本单位，每个进程有独立内存空间互不影响，一个进程至少有一个主线程）；进程创建 4 种方式：① multiprocessing.Process（target 指定任务，底层由 run() 发起，多进程指定不同任务）；② 自定义 Process 子类（重写 run()，多进程完成相同任务）；③ 进程池 Pool（默认为守护进程，同步提交 apply / 异步提交 apply_async，join 阻塞主进程放最后、close 完成任务退出、terminate 立即停止）；④ 进程间通信（Queue 队列访问共同数据，进程池用 multiprocessing.Manager().Queue()）。

### 有什么问题
- **只整理了知识，没写代码**：multiprocessing.Process.py 是空文件，4 种进程创建方式都还没亲手敲代码跑通。
- **进程池方法名拼写要注意**：笔记里写的 `apply_anync` 是笔误，正确是 `apply_async`；`terminater()` 应为 `terminate()`。
- **进程 vs 线程的区别还需要深入**：目前只整理了进程，线程（threading）还没看；GIL 的影响也还没理解。

---

## 薄弱点清单（持续更新，后续出题优先覆盖）

| 来源 | 薄弱点 | 状态 |
| --- | --- | --- |
| D1 摸底 Q5 | 字典计数统计（get/setdefault） | D2 Q7 已攻克 ✓ |
| D1 摸底 Q4 / D2 Q5 | 列表去重 | D2 写出 4 种，待去掉推导式副作用写法 |
| D1 Q6 / D2 Q9 | 整数数位拆分（% //） | D2 已掌握，补数学法 |
| D1 Q8 / D2 Q10 | 字符分类统计 + 不区分大小写计数 | D2 部分完成，待订正 |
| D2 Q2 | 列表推导式条件书写 | 待订正（推进 D3 时跳过了，挂起） |
| D2 Q8 | 二维列表按行/按列聚合（zip*） | 待订正（挂起） |
| D2 小实战 | 列表+字典综合（成绩管理） | 已订正 ✓，但 print 多参数误用（应为 round） |
| D3 Q9 | 递归汉诺塔 | 未完成 |
| D3 Q10 | 函数命名/拼写（analuze→analyze）、输入 q 时重置字典 | 有 bug 待修 |
| D3 Q6 | 递归斐波那契 vs 迭代耗时对比 | 未做 |
| D4 Q4/Q6/Q7/Q8 | 文件操作+异常综合题 | 待还债（合上资料白纸重写） |
| D4 | 自定义异常 MyError 不完整（__init__/__str__ 注释了） | 待完善 |
| D5 | self 是谁 / 类属性vs实例属性 / __str__ return 不是 print | 今日盯防中，练习代码未开始 |
| D5 | 继承 vs 组合判断（Bot 持有 LLMClient 是组合不是继承） | 待练习 Q8 巩固 |
| 进程线程 | 4 种进程创建方式只整理了知识没写代码；线程/GIL 未看 | 待写代码 |
| 通用 | 审题漏输出点、覆盖在用变量、推导式副作用 | 持续盯 |
| 通用 | 函数名/变量名拼写错误（analuze、nam1e、ag1e） | 持续盯，写完检查一遍拼写 |

---

## 下一步
1. **完成 D5 练习**：W1–W7 热身跟写 + Q1–Q8 正式题（重点盯 self/类属性实例属性/__str__ return），Q9/Q10 选做。
2. **还债 D4**：明早 D6 开工前合上资料白纸重写 D4 的 Q4/Q6/Q7/Q8（约 30 分钟）。
3. **补 D2 挂起的订正**：Q2 列表推导式、Q8 二维列表、Q10 不区分大小写字符统计（推进 D3 时跳过了，需回头补）。
4. **修 D3 的 bug**：Q10 函数名 analuze→analyze、get_scores() 输入 q 时不要重置字典；补 Q9 汉诺塔、Q6 斐波那契耗时对比。
5. **D6 模块包 + 高级语法 + 正则**（讲义第 12、13、15 章）。
