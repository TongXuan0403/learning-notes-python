#一、知识速整
#每个点写 "一句话笔记 + 3~5 行最小示例"：

# 1. **序列共性**：索引、切片 `[start:stop:step]`、`len()`、`in`、遍历（列表 / 字符串 / 元组都是序列）
# list,str,tuple。都是有序，可以索引，切片

# 2. **列表 List（可变）**：增 `append/extend/insert` 的区别；删 `pop(按位置)/remove(按值)/del/clear`；
# 改 `lst[i]=x`；查 `index/count/in`；`sort()` 原地排序 vs `sorted()` 返回新列表；
# 嵌套列表；**列表推导式** `[表达式 for x in 序列 if 条件]`；`zip()` 并行遍历


# 3. **字符串 String（不可变）**：`split/join/replace/strip/find/count/startswith`；
# 大小写 `upper/lower`；判断 `isalpha/isdigit/isspace`


# 4. **元组 Tuple（不可变）**：单元素 `(1,)` 逗号不能省；
# 打包解包（`a,b=b,a` 原理）；函数多返回值本质就是元组


# 5. **集合 Set（无序、不重复）**：空集合只能用 `set()`；`add/remove/discard`；
# 交集 `&`、并集 `|`、差集 `-`、对称差 `^`


# 6. **字典 Dict（键唯一）**：`d[k]` 与 `get(k, 默认值)` 的区别；
# `setdefault/update/pop`；遍历 `items()`；`in` 判断的是键


# 7. **收尾**：自己画一张 "列表 / 元组 / 集合 / 字典" 区别表（可变性、是否有序、能否重复、典型用途）


#
# 二、分层题目（4–5h，共 10 题）
#
# 基础（必做）
#
# 1. **列表增删改查**：建 `[3,1,4,1,5]`，依次执行 `append(9)`、`insert(0,0)`、`extend([2,6])`、
# `remove(1)`、`pop()`、`sort()`，每步打印，说出 append 和 extend 的区别


# 2. **列表推导式**：a) 生成 1~100 偶数的平方列表；b) 再筛出能被 3 整除的；c) 把 `["AbC","dEf"]` 全部转小写


# 3. **字符串**：`s = "  Hello, Python World  "`，完成 `strip`、按逗号 `split`、`replace`、`find("Python")`、
# `count("o")`、`upper`；再用 `join` 把 `["2026","09","14"]` 拼成 `"2026-09-14"`


# 4. **元组解包**：写函数 `min_max(nums)` 一次返回最小值和最大值，调用处解包成两个变量；
# 再验证为什么 `(1)` 不是元组而 `(1,)` 是


# 5. **集合运算**：`a={1,2,3,4}`、`b={3,4,5,6}`，输出交 / 并 / 差集；
# 给 `[1,2,2,3,3,3]` 去重 —— 写出**三种**写法（对比你 D1 卡住的 Q4）


# 6. **字典基础**：建一个学生字典，演示 `d[k]`、`get(键, 默认值)`、`update`
# 合并、`pop` 删除、用 `items()` 遍历打印 "姓名: xx，分数: xx"


#
# 进阶
#
# 7. **词频统计（摸底 Q5，今天必须拿下）**：统计 `"the cat sat on the mat the cat"`
# 中每个单词的次数，并按次数**降序**输出（`get/setdefault` 计数 + `sorted(items, key=lambda x: x[1], reverse=True)`）


# 8. **二维列表**：`[[88,76,92],[66,77,88],[99,91,85]]`，
# 输出每人平均分、每科平均分、总平均分（提示：`zip(*scores)` 可以按列取）


# 9. **薄弱点回炉①（数位）**：写函数 `digits(n)` 把整数每位拆成列表（`12345 → [1,2,3,4,5]`）；
# 再用列表收集 100~999 全部水仙花数，重做 D1 的 Q6 并核对结果应为 153/370/371/407


# 10. **薄弱点回炉③（字符统计）**：重做 D1 的 Q8（字母 / 数字 / 空格 / 其他计数），
# 并额外统计每个字母（不区分大小写）出现次数存入字典，输出出现次数最多的字母


#
# **小实战**：学生成绩管理 —— 循环输入 "姓名 成绩"，输入 `q` 结束，输出人数、总分、平均分、最高分学生姓名、
# 按成绩降序的完整名单（列表 + 字典综合，面向对象案例的前置练习）




